const fetch = require('node-fetch');

async function testCreateRoom() {
  const token = "put_token_here"; // We don't have the user's token... wait, can we just make a generic request to see if the endpoint is reachable?
}
